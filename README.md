# funcnet 

This is an educational project to get familiar with artificial neural networks and functional programming.
[Haskell Tool Stack](https://docs.haskellstack.org/en/stable/README/) is required to build and run the executables.

## Run the project
There are different executables that can be run with:

    stack run <exe-name> (e.g. stack run funcnet-exe)
